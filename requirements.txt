fastapi>=0.115.0
uvicorn[standard]>=0.32.0  # 包含 websockets 等完整依赖
pydantic>=2.9.0
pydantic-settings>=2.6.0
httpx>=0.27.0
python-dotenv>=1.0.0

# 网页工具依赖
duckduckgo-search>=6.0.0   # web_search: DuckDuckGo 搜索（无需 API Key）
readability-lxml>=0.8.1    # web_fetch: 网页正文提取（可选，有则优先使用）
beautifulsoup4>=4.12.0     # web_fetch: HTML 解析

# 测试依赖
pytest>=8.0.0
pytest-asyncio>=0.23.0
